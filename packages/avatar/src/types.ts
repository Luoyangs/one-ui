
export interface AvatarOptions {
  /**
   * 支持one-svgs
   */
  icon?: string;
  /**
   * 支持传统的img
   */
  src?: string;
  /**
   * 圆形头像
   */
  circle?: boolean;
  /**
   * 支持多种样式
   */
  size: string;
}
